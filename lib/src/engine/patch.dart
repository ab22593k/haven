import '../provider.dart';

Future<void> applyEnginePatches({
  required Scope scope,
  required String engineCommit,
}) async {
  // final config = HavenConfig.of(scope);
  // final git = GitClient.of(scope);
  // final baseCache = config.getFlutterCache(engineCommit, patched: false);
  // final patchedCache = config.getFlutterCache(engineCommit, patched: true);
}
